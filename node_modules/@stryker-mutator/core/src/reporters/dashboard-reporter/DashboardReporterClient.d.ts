import { Logger } from '@stryker-mutator/api/logging';
import { HttpClient } from 'typed-rest-client/HttpClient';
export interface StrykerDashboardReport {
    apiKey: string;
    repositorySlug: string;
    branch: string;
    mutationScore: number;
}
export default class DashboardReporterClient {
    private readonly log;
    private readonly dashboardReporterClient;
    static inject: ["logger", "httpClient"];
    constructor(log: Logger, dashboardReporterClient: HttpClient);
    postStrykerDashboardReport(report: StrykerDashboardReport): Promise<void>;
}
//# sourceMappingURL=DashboardReporterClient.d.ts.map