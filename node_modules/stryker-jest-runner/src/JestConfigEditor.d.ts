import { Config, ConfigEditor } from 'stryker-api/config';
import { Logger } from 'stryker-api/logging';
export default class JestConfigEditor implements ConfigEditor {
    private readonly log;
    static inject: ["logger"];
    constructor(log: Logger);
    edit(strykerConfig: Config): void;
    private getConfigLoader;
    private overrideProperties;
}
//# sourceMappingURL=JestConfigEditor.d.ts.map